## functions
# sidediff
function sidediff
  diff --color -y <(fold -s -w $argv[3] $argv[1]) <(fold -s -w $argv[3] $argv[2])
end

# wn
# function nv
#   watch -n ${1:-1} nvidia-smi
# end
# ps aux
# psa () {
# ps aux | grep $1
# }
# docker
# dockersudo () {
# sudo usermod -aG docker $1 && newgrp docker
# }
